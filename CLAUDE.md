# Log Viewer — Claude Context

Serviço leve de visualização de logs do EvoNexus via browser.

Repositório: `log-viewer`  
Serviço: `log-viewer.service`  
Porta: configurada via systemd

---

## Estrutura

Projeto mínimo — arquivo único (`server.py`) + scripts de start/stop + systemd.

Documentação: apenas `README.md` na raiz (projeto simples, sem `docs/` necessário).

Nunca criar subpastas de docs aqui — qualquer documentação vai no `README.md`.
